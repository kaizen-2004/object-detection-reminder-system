# This file ensures the models directory is tracked in git
# Users should place their YOLO model files (*.pt) in this directory
# The .gitignore will exclude the actual model files but keep this directory structure 